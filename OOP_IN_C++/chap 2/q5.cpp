#include <iostream>
#include <ctype.h>
using namespace std;

int main(){
    char x;
    cin >> x;
    cout << islower(x);

    /*
    Returns 0 is uppercase 
    else it returns a non zero intiger
    */

    return 0;
}